# Forge AI MVP 使用说明

> 这是一份手把手的使用 walkthrough。命令参考和环境变量完整列表见 [README.md](README.md)。

## 这是什么（30 秒）

Forge AI 是一个**多 Agent 协作生产平台**：让多个 AI Agent 在一个可靠中间层下接力完成一次内容生产任务，系统独立掌握"生产到哪了、哪些成果有效、什么问题没解决"，交付由系统门禁决定而非 Agent 自说自话。

典型链路（歌词场景，Fake Pi 7 个 Turn 跑完）：

```
总控派任务 → 生成 v1 → 审核挑刺 → 总控下发定点返修(editable/frozen) → 生成 v2 → 复审通过 → 申请交付 → 系统门禁核对 → approved
```

歌词只是示例，换"文案+质检"骨架不变（见下"换场景"）。

---

## 1. 环境准备

需要 Node ≥ 20（实测 v24）、npm 11。

```bash
npm install
```

> 必须本机安装，不能拷贝 `node_modules`。如果 better-sqlite3 / esbuild 的 native 二进制缺失（报 `Could not locate the bindings file` 之类），补一次：
> ```bash
> npm rebuild better-sqlite3 esbuild
> ```

---

## 2. 五分钟跑通（Fake Pi 歌词）

Fake Pi 用脚本驱动模型，不花钱、不联网、几秒跑完，用来验证全链路。

```bash
PI_MODE=fake DB_PATH=./data/song.db SCENARIO_PATH=./scenarios/songwriting/scenario.yaml npm run dev
```

**终端会依次打印**（关键行）：

```
[Case] 创建: case_xxx
[Case] 启动: case_xxx
[Turn 1] Agent: supervisor (总控)
[Turn 2] Agent: generator (生成)
  [产物] v1 已发布
[Turn 3] Agent: reviewer (审核)
[Turn 4] Agent: supervisor (总控)
[Turn 5] Agent: generator (生成)
  [产物] v2 已发布
[Turn 6] Agent: reviewer (审核)
[Turn 7] Agent: supervisor (总控)
[交付] 门禁通过，Case 已交付！
[最终] Case case_xxx 状态: approved
```

跑完数据落在 `./data/song.db`。接下来用 Web 页看刚才发生了什么。

---

## 3. 启动 Web 回放页

新开一个终端：

```bash
cd apps/web && DB_PATH=../../data/song.db npm run dev
```

浏览器打开 http://localhost:3000 。

> `DB_PATH` 要用**绝对路径或相对 apps/web 的路径**指向刚跑完的 db。也可以直接 `./scripts/web.sh`（默认读 `./data/forge.db`，用 `DB_PATH=` 覆盖）。

### 页面上能看到什么

- **左侧任务栏**：列出 db 里所有 Case，状态圆点（绿=approved / 蓝=running / 黄=waiting / 红=stopped/failed）。点切换。
- **泳道时间线**：每个 Agent 一条泳道（总控/生成/审核），Turn 卡片按顺序排在各自泳道里，**SVG 箭头**连出路由关系，颜色按类型：蓝=系统派发、橙=返修、绿=交付。
- **Turn 卡片**：本轮输入 / 工具调用（chips）/ 产出产物版本 / **"敏感诊断·默认折叠"**（点开展开模型实际收到的 system+user 消息原文，铁律 6 默认折叠）。
- **产物版本 diff**：行级对比，标出哪些行被改（橙）、哪些是 editable 可改（蓝）、哪些是 frozen 冻结（灰）、越界违规（红）。
- **Issue 生命周期**：`created → repairing → claimed_fixed → verified`，带证据/锚点。
- **交付门禁**：5 项逐项 pass/fail（版本有效 / 审核通过 / blocking Issue verified / 无运行中返修 / 无未完成 Turn）。
- **顶部横幅**：门禁未过时显示 FAIL CLOSED 横幅；Case 停在 `waiting_human` 时显示人工横幅。

页面是**只读轮询**（非 SSE），不调 Pi，只读 SQLite。

---

## 4. 换个场景（证明配置驱动）

不碰任何源码，只换一个 YAML，跑文案场景（writer/qc，产物是 copy 而不是 lyrics）：

```bash
PI_MODE=fake DB_PATH=./data/copy.db SCENARIO_PATH=./scenarios/copywriting/scenario.yaml npm run dev
```

终端里 Agent 变成 writer/qc，产物类型变成 copy。用 Web 页指到 `./data/copy.db` 看，泳道标签、产物类型、门禁的 deliverable 都跟着变。**这就是支柱四"配置驱动，不写死业务"的实证**——平台代码零业务分支，新场景只写 YAML + 提示词 + Fake Pi 脚本。

---

## 5. 自定义输入

新 Case 的输入不再写死在源码（铁律 1）。三种方式，按优先级：

```bash
# 方式 1：环境变量传 JSON（字段对应当前场景的 input_fields）
FORGE_INPUT='{"reference_lyrics":"我的歌词A\n我的歌词B","fixed_phrase":"我的金句"}' \
  PI_MODE=fake DB_PATH=./data/custom.db SCENARIO_PATH=./scenarios/songwriting/scenario.yaml npm run dev

# 方式 2：JSON 文件（多行内容推荐）
echo '{"reference_lyrics":"...","fixed_phrase":"..."}' > /tmp/input.json
FORGE_INPUT_FILE=/tmp/input.json PI_MODE=fake DB_PATH=./data/custom.db \
  SCENARIO_PATH=./scenarios/songwriting/scenario.yaml npm run dev

# 方式 3：都不设 -> 自动读 <scenario 目录>/input.example.json
#   scenarios/songwriting/input.example.json 和 copywriting/input.example.json 已自带
```

字段不齐会 **fail loud**：`输入缺少必填字段：fixed_phrase`。每个场景声明的字段在 `scenario.yaml` 的 `input_fields` 里。

---

## 6. 接真实模型

真实 Pi 调 DeepSeek，花钱、慢（每个 Turn 一次真实 API 调用），但验证"模型在最小约束下能否稳定协作"这个核心假设。

```bash
PI_MODE=real DEEPSEEK_API_KEY=sk-xxxx PI_MODEL_ID=deepseek-v4-flash \
  DB_PATH=./data/real.db SCENARIO_PATH=./scenarios/songwriting/scenario.yaml npm run dev
```

- `DEEPSEEK_API_KEY` 只走环境变量，**不进日志/DB/前端**（铁律 6）。
- `PI_MODEL_ID` 可选 `deepseek-v4-flash`（快/便宜）或 `deepseek-v4-pro`（强/贵）。
- deepseek 是推理模型，会返回 thinking block；adapter 已做空响应重试（最多 3 次）。
- 真实模型偶发不稳定（不服从 scope、审核证据不可靠），这是 2.2 验收明确允许的，调提示词后重跑即可。

> 国内网络访问 GitHub 不影响真实 Pi（DeepSeek API 直连）。只有 `git fetch/push` 才需要代理。

---

## 7. 崩溃恢复演示

**Fake Pi**（进程级 kill + 重启，非内存模拟）：

```bash
node scripts/crash-recovery-e2e.cjs
```

脚本自动：跑 3 个 Turn → `taskkill /F` 杀进程 → 同 DB 重启 → 从 Turn 4 续跑 → approved。打印 8 项断言全 YES。

**真实 Pi kill -9 + persistent session 跨进程历史续跑**（2.3 硬指标，需要 API Key，慢）：

```bash
DEEPSEEK_API_KEY=sk-xxxx node scripts/crash-recovery-realpi-e2e.cjs
```

9 项断言全 YES，含 persistent session `.jsonl` 跨进程条目增长（7→23）、已完成 Turn 哈希不变、最终 approved。

---

## 8. 核心概念速查

| 概念 | 含义 |
|---|---|
| **Case** | 一次完整生产任务。状态：running / waiting_review / repairing / waiting_human / waiting_recovery / approved / failed / stopped |
| **Turn** | 一个 Agent 的一轮调用。事务化原子提交（queued→running→completed），崩溃回滚不残留 |
| **Agent** | 配置里的角色（songwriting: supervisor/generator/reviewer；copywriting: writer/qc）。session 策略 persistent（跨轮记忆）/ cold_per_version（每轮新开） |
| **产物版本** | append 不覆盖。v1 superseded / v2 delivered。content_hash 标识内容 |
| **Issue** | 审核挑出的问题。状态机 open→repairing→claimed_fixed→verified。**claimed_fixed 不算关闭，只有 verified 算** |
| **返修指令** | editable_anchors（可改行）/ frozen_anchors（冻结行）。越界修改的版本 rejected，不进复审 |
| **交付门禁** | 5 项独立核对。Agent 调 `approve_delivery` ≠ 交付，门禁全绿才 approved |
| **5+1 工具** | publish_artifact / submit_evaluation / route_message / approve_delivery + request_human_input（降级停 waiting_human）+ read（受限，仅 skills 目录白名单） |

---

## 9. 命令速查

```bash
# 检查与测试
npm run check            # tsc --noEmit，0 错误
npm run test             # vitest，78 passed

# 跑场景
PI_MODE=fake DB_PATH=./data/song.db SCENARIO_PATH=./scenarios/songwriting/scenario.yaml npm run dev
PI_MODE=fake DB_PATH=./data/copy.db SCENARIO_PATH=./scenarios/copywriting/scenario.yaml npm run dev

# 真实 Pi
PI_MODE=real DEEPSEEK_API_KEY=sk-xxxx DB_PATH=./data/real.db SCENARIO_PATH=./scenarios/songwriting/scenario.yaml npm run dev

# Web 回放页
cd apps/web && DB_PATH=../../data/song.db npm run dev   # http://localhost:3000

# 崩溃恢复测试
node scripts/crash-recovery-e2e.cjs
DEEPSEEK_API_KEY=sk-xxxx node scripts/crash-recovery-realpi-e2e.cjs
```

---

## 10. 常见问题

| 现象 | 原因 / 解决 |
|---|---|
| `Could not locate the bindings file` / better-sqlite3 报错 | native 二进制缺失，`npm rebuild better-sqlite3 esbuild` |
| 真实 Pi 报 `需要设置 DEEPSEEK_API_KEY` | 没传环境变量。铁律 6：Key 只走 env，不写死 |
| 真实 Pi 某轮卡住、空响应 | deepseek 偶发只返回 thinking。adapter 已加 3 次重试；若仍卡，换 `PI_MODEL_ID=deepseek-v4-pro` 或调提示词 |
| Web 页打不开 / 报 DB 错 | `DB_PATH` 没指对，要用相对 `apps/web` 的路径或绝对路径 |
| 端口 3000 被占 | `npm run dev -- -p 3137` 换端口 |
| `git fetch/push` 超时 | 国内网络，需代理。本仓库已配 `http.proxy=127.0.0.1:10808`（仅 local config），代理关了用 `git config --unset http.proxy && git config --unset https.proxy` 清掉 |
| Case 卡在 `waiting_human` | Round 2 已支持恢复：CLI `forge case resume <id> --answer '...'` 或 Web 页面输入框提交 |

---

## 11. CLI 操作系统（Round 2 新增）

Round 2 新增 `forge` CLI，可在任意目录操作 Case：

### 安装
CLI 已集成在 monorepo 中，`npm install` 后即可使用。

### 命令速查
```bash
# 模板管理
npx forge template list              # 列出可用模板
npx forge template show songwriting  # 查看模板详情
npx forge template validate songwriting  # 校验模板配置

# Case 生命周期
npx forge case create --template songwriting --input '{"reference_lyrics":"...","fixed_phrase":"..."}' --db ./data/my.db
npx forge case run <case_id> --wait --db ./data/my.db
npx forge case status <case_id> --db ./data/my.db
npx forge case list --db ./data/my.db
npx forge case stop <case_id> --db ./data/my.db
npx forge case resume <case_id> --answer '我的回答' --db ./data/my.db

# 产物与门禁
npx forge artifact get <artifact_id> --db ./data/my.db
npx forge diff <case_id> --db ./data/my.db
npx forge gate <case_id> --db ./data/my.db

# 真实 Pi
npx forge case run <case_id> --wait --mode real --db ./data/my.db
```

### stdout 协议
- `case run` 第 1 行：`{"case_id":"..."}`（同步 flush）
- `case run` 末行：完整结果 JSON（含 success/final_artifact/gate）
- 错误时：`{"error":"...","blocking_case_id":"..."}` + exit 非 0

### 日志
- 运行日志写入 `data/case-<case_id>.log`
- 进度信息走 stderr，stdout 只有协议 JSON

---

## 12. UI 操作系统（Round 2 新增）

Web 页面从"只读回放"升级为"可操作系统"：

### 启动
```bash
cd apps/web && npm run build && npm run start
# 浏览器打开 http://localhost:3000
```

> **注意**：验收用 `next start`（稳定）。`next dev` 仅开发用，热重载可能杀 detached 子进程，被杀的 case 留 running 状态可用 CLI 续跑。

### 功能
- **选模板**：下拉选择 songwriting/copywriting
- **填输入**：根据模板 input_fields 动态生成表单
- **跑 Case**：一键创建并运行
- **看进度**：5 秒轮询展示状态
- **恢复 waiting_human**：输入框提交答案，Case 恢复运行

---

## 数据落盘位置

- `data/*.db` - SQLite 数据库（每个场景/测试一个，gitignored）
- `data/pi-sessions/` - 真实 Pi 的 persistent session 文件（`.jsonl`，跨进程恢复用，gitignored）
- 证据 DB：`data/real-pi-multi.db`（16 个真实 Case）、`data/crash-realpi.db`（kill-9 恢复证据）

## 入口文档

想深入看设计：`CLAUDE.md`（项目入口）→ `AGENTS.md`（六条铁律）→ `Forge_AI_MVP_需求文档.md`（完整规格）→ `交付标准.md`（验收清单）。
