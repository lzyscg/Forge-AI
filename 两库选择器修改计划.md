# 两库（生产/测试）+ 选择器 修改计划

> 把"一堆散库"收敛为两个命名库 + CLI/UI 选择器 + "全部"聚合视图。本文档是实施前定稿方案。

## 1. 目标

- 两个命名库：`production`（生产）/ `test`（测试）。
- CLI + UI 都能选库（手动切换生产/测试）。
- 支持"全部"聚合查看两个库的 Case（只读）。
- 默认 env = production。

## 2. 两库模型 + env 配置

| env | DB 路径 | 用途 |
|---|---|---|
| `production` | `data/production.db` | 正式生产 Case |
| `test` | `data/test.db` | 测试 / 临时验证 |
| `all` | （聚合两个） | 只读查看两个库 |

**配置位置**：`packages/adapters/src/paths.ts`（共享，CLI/worker/web 都引）。

新增导出：
- `DB_ENVS`：`{ production: 'data/production.db', test: 'data/test.db' }`（路径可用 `FORGE_DB_PRODUCTION` / `FORGE_DB_TEST` 环境变量覆盖）。
- `DbEnv = 'production' | 'test' | 'all'`，`SingleDbEnv = 'production' | 'test'`。
- `resolveDbPaths(env): string[]`：production/test 返回单元素，all 返回两个（绝对路径，基于包根）。
- `resolveSingleDbPath(env): string`：写操作用，all 非法（抛错）。
- `defaultDbEnv(): SingleDbEnv`：默认 production，`FORGE_ENV` 环境变量可覆盖。

**硬编码两个 env**（不做 N 库可扩展，简单）。以后要多库再重构。

## 3. CLI 改动

- 所有 `case` / `artifact` / `diff` / `gate` 命令加 `--env <production|test|all>` flag。
- 解析优先级：`--db <路径>`（显式覆盖）> `--env` > 默认 production。
- **写操作**（`case create/run/resume/stop`）：env 必须单库（production/test）。`--env all` -> 报错"写操作必须指定单个库（--env production|test）"。
- **读操作**（`case list/status`、`artifact get`、`diff`、`gate`）：
  - `--env production|test` -> 查该库。
  - `--env all` -> 聚合两个库。`list` 合并 Case 列表（每个 Case 带 `dbPath` 来源标签）；`status/artifact/diff/gate` 按 case_id 在两个库找，命中哪个用哪个（找不到报错）。
- `template list/show/validate` 不碰 DB，不加 `--env`。

## 4. Web 改动

### 4.1 顶部 env 下拉
- 在 topbar 右侧加下拉（生产 / 测试 / 全部），全局可见。
- 切换改 URL `?env=production|test|all`，页面按 env 重渲染。默认 production。

### 4.2 lib/db.ts 改造
- `getCases(env)`：env 解析成 1 或 2 个库路径；`all` 时开两个库、合并 Case 列表、**每个 Case 打 `dbPath` 标签**（标明来自哪个库）。按 created_at 排序。
- 详情函数（`getCase` / `getTurns` / `getArtifacts` / `getIssues` / `getGate` 等）：**加 `dbPath` 参数**，查指定库。页面用选中 Case 的 `dbPath` 调详情。
- **去掉 `.env.local` 的 `DB_PATH`**（被 env 选择器取代）。

### 4.3 page.tsx
- 从 URL 读 env（默认 production）。
- `getCases(env)` 取列表；选中 Case 后用该 Case 的 `dbPath` 调详情函数。

## 5. 聚合逻辑（关键设计，避免复杂度爆炸）

- 聚合**只在 Case 列表层**做（`getCases(all)` 合并两个库的 cases，打 `dbPath` 标签）。
- 选中某 Case 后，**详情用该 Case 的 `dbPath` 查单个库**--不做跨库 join，不处理跨库关联。
- case_id 跨库冲突：UUID，理论不冲突；若真冲突，列表显示两条（dbPath 不同），可接受。

## 6. 种子数据

- `data/production.db` <- 复制 `data/story-runs/gaokao-zero-real-003/forge.db`（9 个故事 Case，含恢复的 `case_42376dcd693f43a1`）。
- `data/test.db` <- 复制 `data/r2-song.db`（Fake Pi 歌词 Case，有点数据好测，非空）。

## 7. 改动文件清单

| 文件 | 改动 |
|---|---|
| `packages/adapters/src/paths.ts` | 加 `DB_ENVS` / `DbEnv` / `resolveDbPaths` / `resolveSingleDbPath` / `defaultDbEnv` |
| `apps/cli/src/setup.ts`（或新 helper） | `--env` flag 解析 + dbPath 解析逻辑 |
| `apps/cli/src/commands/case.ts` | 用解析的 dbPath；`list/status` 支持 `all` 聚合 |
| `apps/cli/src/commands/{artifact,diff,gate}.ts` | `--env` + `all` 搜索 |
| `apps/web/lib/db.ts` | `getCases(env)` 聚合 + 详情函数加 `dbPath` 参数 |
| `apps/web/app/page.tsx` | env 从 URL 读，传 `getCases`，详情用 `case.dbPath` |
| `apps/web/app/env-selector.tsx`（新） | 顶部下拉 |
| `apps/web/app/globals.css` | env 下拉样式 |
| `apps/web/.env.local` | 去掉 `DB_PATH` |
| `data/production.db` / `data/test.db` | 种子（gitignore，本地） |

## 8. 不在范围

- 迁移所有旧库（`real-pi-multi.db` 等留原处，不自动导入；只手工 seed production/test）。
- 多用户 / 远程 DB（仍本地 SQLite）。
- N 库可扩展（硬编码两个）。
- 跨库 Case 关联查询（聚合只在列表层）。

## 9. 已定的决策

1. **env 下拉位置**：topbar 右侧（全局可见，和品牌标并排）。
2. **`--env all` 写操作**：报错（强制明确选库），不静默默认 production。
3. **test.db 初始**：复制 `data/r2-song.db`（Fake Pi 歌词，非空好测）。

## 10. 验收

- `forge case list --env production` -> 9 个故事 Case；`--env test` -> 歌词 Case；`--env all` -> 两个库合并。
- Web 下拉切生产/测试/全部，"全部"显示两个库的 Case；点任一 Case 详情正常（从对应库读）。
- `npm run check` 0 错误 / `npm test` 无回归（113 passed）。
- 写操作 `--env all` 报错；`--db` 显式覆盖仍可用。
- 旧的 `--db` / `DB_PATH` 行为不破坏（`--db` 优先级最高；`DB_PATH` 在 web 被 env 取代但 CLI `--db` 保留）。
